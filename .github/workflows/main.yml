name: GRADLE_DEMO

on:
  push:
    branches: 
      - main

jobs:
  testing:
    runs-on: ubuntu-latestqwe
    
    steps:
      - name: Print Hello Message in Testing
        run : echo "Hello World"
